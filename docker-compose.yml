version: "3.9"
services:
  db:
    image: postgis/postgis:16-3.4
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: conflictpulse
      POSTGRES_PASSWORD: conflictpulse
      POSTGRES_DB: conflictpulse
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:
